# 抽卡核心機率與演算法分析筆記

## 一、 基礎經濟學與期望值

* **定價與平均成本：**

$$\text{定價} = \text{平均成本} = \text{單抽價格} \times \text{平均抽數} = \frac{\text{單抽價格}}{\text{綜合出貨率 } P_{\text{total}}}$$

* **無保底（幾何分布）下的期望值：**

$$E = \frac{1}{p} \quad (\text{此時綜合出貨率 } P_{\text{total}} = \text{基礎機率 } p)$$

* **全圖鑑預期抽數（贈券收集者問題）：**
  
  若池內有 $N$ 個概率均等的目標角色，且單抽獲得目標星級的基礎機率為 $p$：
  
$$E_{\text{全圖鑑}} = \frac{1}{p} \times N \sum_{n=1}^{N} \frac{1}{n}$$
  
> **注意：** 此公式成立的前提是「全圖鑑所有角色出貨機率完全均等（均勻分布）」，且不考慮大保底或 UP 池分流。

* **參考資料：**
[总是吃保底？手游抽卡中的数学秘密，学会了再也不被骗氪！](https://www.bilibili.com/video/BV1Ye4y1p7oE)

---

## 二、 保底機制下的期望值

### 1. 硬保底模型（Fixed Cap）
在第 $N$ 抽前機率皆為固定值 $p$，若前 $N-1$ 抽皆沒中，第 $N$ 抽強迫中獎（機率為 100%）。
其期望值 $E$ 可透過生存函數（Survival Function）簡化，其閉合形式為：

$$E = \sum_{n=0}^{N-1} (1-p)^n = \frac{1-(1-p)^N}{p}$$

### 2. 軟保底模型（Soft Cap）
基礎機率 $p_n$ 隨抽數 $n$ 動態變化（例如：前 73 抽為 0.6%，第 74 抽起每抽遞增 6% 直到第 90 抽為 100%）。

* **期望值公式：** 必須視 $p_n$ 的遞增曲線而定，無法化簡，只能用數值法窮舉求和：

$$E = \sum_{n=1}^{N} n \cdot P(X=n)$$

  其中，剛好在第 $n$ 抽中獎的獨立機率為（即前 $n-1$ 抽皆未中，第 $n$ 抽中獎）：
$$P(X=n) = p_n (1-p_1)(1-p_2)\cdots(1-p_{n-1}) = p_n \prod_{i=1}^{n-1}(1-p_i)$$
* **綜合機率：** $P_{\text{total}} = \frac{1}{E}$

### 3. 反推基礎機率（數值逼近）
當已知期望抽數 $E$（或綜合機率 $P_{\text{total}}$）與保底數 $N$，想要求出對應的基礎機率 $p$ 時，無法直接代入公式求解，必須使用**二分法**或**牛頓法**進行數值逼近。

---

## 三、 機率波動與區間機率

* **$n$ 抽內「至少」中獎一次的機率：**

$$P_{\text{至少中一次}} = 1 - (1-p)^n$$

* **$n$ 抽內「完全沒」中獎的機率：**

$$P_{\text{完全沒中}} = (1-p)^n$$

---

## 四、 統計學分布與圖形可視化

* **PMF 曲線（機率質量函數）：** 顯示「剛好在第 $n$ 抽」出貨的獨立機率。在傳統幾何分布下是一條緩慢下降的曲線；但在**軟保底模型**下，PMF 會在單抽機率遞增的區間（如 74-80 抽）**衝出一個極高的峰值**，隨後因剩餘樣本極少而驟降，並在硬保底 $N$ 抽處形成最後的機率斷崖，自 $N+1$ 抽起歸零。
* **CDF 曲線（累積分布函數）：** 一條從 0 爬升到 100% 的階梯狀/平滑曲線，代表 $n$ 抽內出貨的總機率。數值上等於 PMF 中從第 1 抽累加到當前抽數的所有面積總和。
* **單抽機率與累積機率的誤區：** 雖然累積機率（CDF）隨抽數上升，且在無保底時全局出貨率（PMF）隨抽數遞減；但在進入保底前，**「已知前面沒中，下一抽會中的條件機率」**其實是完全平的（固定為 $p$）。
* **非酋標籤：** 透過 CDF 分布，可以幫玩家標註他的非歐位置。例如當 CDF 達到 98% 時仍未出貨，剩下的 2% 極端運氣差的人就是俗稱的「非酋」。

> * **幾何分布 (Geometric Distribution)：** 「從 0 開始抽，直到第一次出貨為止的過程分布」（算抽到第一次成功要花幾抽）。
> * **二項分布 (Binomial Distribution)：** 「當機率固定為 $p$ 時，在 $n$ 次嘗試下，成功次數的分布」（算 $n$ 抽內中 2 隻或 3 隻的機率）。

---

## 五、 進階：動態偽隨機與隱馬可夫（Hidden Markov）黑箱機制猜想

當遊戲廠商不滿足於傳統的獨立隨機事件時，為了最大化商業營收（LTV）與留存率，可能會引入**隱馬可夫模型（HMM）**或**馬可夫切換模型（Markov Switching Model）**。

玩家在後台系統中，會基於其行為數據在不同的**「隱藏狀態（Hidden Regimes）」**間轉換：

*   **狀態 $S_t \in \{ \text{歐皇狀態 (新手/欲退坑)}, \text{常規狀態}, \text{非酋狀態 (核心重課)} \}$**
*   狀態之間的轉換由**轉移機率矩陣（Transition Matrix）** $\mathbf{P}$ 控制，轉移權重取決於玩家的時間軸與行為特徵（如：近期課金量、上線頻率、連續未出貨抽數）。

### 1. 隱藏狀態下的動態機率方程

真實的出貨機率並非固定值，而是由當前系統所處的隱藏狀態 $S_t$ 與池子自帶的保底機制動態疊加（通常採覆蓋或步進累加邏輯）：

$$P(\text{出貨} \mid S_t = j, \text{抽數 } n) = \min\big(1, \, p_j + \Delta\alpha(n)\big)$$

*   $p_j$：該狀態下的基礎機率（例如 $p_{\text{歐皇}} = 1.5\%$, $p_{\text{非酋}} = 0.3\%$）。
*   $\Delta\alpha(n)$：池子自帶的保底修正增量（即第二章所述的軟/硬保底遞增機制，在未達軟保底門檻前為 0）。

### 2. 演算法介入的「特徵碼（Signature）」

由於玩家無法直接觀測後台的隱藏狀態 $S_t$，只能藉由觀測隱含狀態產生的「發射物（Emissions）」，即**抽卡序列結果**。

*   **動態控率特徵：** 當觀測到極低機率的事件（如連續數次大保底、極端連黑）在時間軸上**成群出現（Volatility Clustering）**，隨後又突然莫名連續「雙黃蛋」，這種不符合幾何分布獨立性的規律波動，即為後台狀態切換矩陣介入的演算法特徵碼。
*   **核心變數：** 玩家的體感出貨率，本質上是動態狀態轉移矩陣與歷史抽卡序列動態交織後的非獨立隨機結果。
